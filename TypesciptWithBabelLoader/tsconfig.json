{
  "compilerOptions": {
    "outDir": "./dist/",
    "noImplicitAny": true,
    "module": "es6", // accepts ES6 Module system.
    "target": "es5",
    "jsx": "react",
    "allowJs": true,
    "moduleResolution": "node" // resolves to node module system that is understandable by node.
  }
}
